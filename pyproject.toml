[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "volatility_surface"
version = "0.1.0"
dependencies = [
    "numpy",
    "pandas",
    "scipy",
    "plotly",
    "matplotlib"
]
